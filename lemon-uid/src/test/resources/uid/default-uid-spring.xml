<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">

    <!-- UID generator -->
    <!--<bean id="disposableWorkerIdAssigner" class="com.baidu.fsg.uid.worker.DisposableWorkerIdAssigner"/>-->

    <bean id="fixedWorkerIdAssigner" class="org.lemonframework.uid.worker.FixedWorkerIdAssigner" />

    <bean id="defaultUidGenerator" class="org.lemonframework.uid.impl.DefaultUidGenerator" lazy-init="false">
        <property name="workerIdAssigner" ref="fixedWorkerIdAssigner"/>

        <!-- Specified bits & epoch as your demand. No specified the default value will be used -->
        <property name="timeBits" value="29"/>
        <property name="workerBits" value="21"/>
        <property name="seqBits" value="13"/>
        <property name="epochStr" value="2016-09-20"/>
        <property name="workerIdFile" value="/Users/apple/workerId"/>
    </bean>

    <!-- Import mybatis config -->
    <!--<import resource="classpath:/uid/mybatis-spring.xml"/>-->

</beans>
